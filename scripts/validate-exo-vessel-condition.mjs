import fs from 'node:fs/promises';
import path from 'node:path';
import process from 'node:process';
import vm from 'node:vm';

const root=process.cwd();
const files=[
  'blacklight-exo-ftl-physics-definitions.js','blacklight-exo-ftl-operational-definitions.js','blacklight-exo-ftl-runtime.js','blacklight-exo-ftl-core.js','blacklight-exo-ftl-engineering-extension.js',
  'blacklight-exo-ftl-path-level-core.js','blacklight-exo-ftl-path-level-paths-physical.js','blacklight-exo-ftl-path-level-paths-dimensional.js','blacklight-exo-ftl-path-level-paths-discrete.js','blacklight-exo-ftl-path-level-runtime.js','blacklight-exo-ftl-path-level-engineering.js','blacklight-exo-ftl-path-level-controller.js',
  'blacklight-exo-ftl-mechanism-core.js','blacklight-exo-ftl-mechanism-path-inertial-torch.js','blacklight-exo-ftl-mechanism-path-metric-envelope.js','blacklight-exo-ftl-mechanism-path-gravitic-plane.js','blacklight-exo-ftl-mechanism-path-slipstream-shear.js','blacklight-exo-ftl-mechanism-path-q-lattice.js','blacklight-exo-ftl-mechanism-path-n-manifold.js','blacklight-exo-ftl-mechanism-path-fold-jump.js','blacklight-exo-ftl-mechanism-path-wormhole-gate.js','blacklight-exo-ftl-mechanism-path-phase-displacement.js','blacklight-exo-ftl-mechanism-runtime.js',
  'blacklight-exo-ftl-assembly-core.js','blacklight-exo-ftl-assembly-runtime.js','blacklight-exo-ftl-calculation-core.js','blacklight-exo-ftl-calculation-performance.js','blacklight-exo-ftl-calculation-energy.js','blacklight-exo-ftl-calculation-runtime.js','blacklight-exo-ftl-certification-core.js','blacklight-exo-ftl-certification-route.js','blacklight-exo-ftl-certification-reliability.js','blacklight-exo-ftl-certification-runtime.js',
  'blacklight-exo-vessel-definitions.js','blacklight-exo-vessel-biology.js','blacklight-exo-vessel-core.js','blacklight-exo-vessel-philosophy-definitions.js','blacklight-exo-vessel-manufacturer-definitions.js','blacklight-exo-vessel-manufacturer-runtime.js','blacklight-exo-vessel-philosophy-runtime.js','blacklight-exo-vessel-engineering-definitions.js','blacklight-exo-vessel-engineering-runtime.js','blacklight-exo-vessel-armor-distribution.js','blacklight-exo-vessel-module-definitions.js','blacklight-exo-vessel-voxel-definitions.js','blacklight-exo-vessel-voxel-core.js','blacklight-exo-vessel-voxel-routing.js','blacklight-exo-vessel-contracts.js','blacklight-exo-vessel-module-runtime.js','blacklight-exo-vessel-module-hardening.js','blacklight-exo-vessel-voxel-runtime.js','blacklight-exo-vessel-voxel-armor.js','blacklight-exo-vessel-voxel-contracts.js','blacklight-exo-vessel-condition-definitions.js','blacklight-exo-vessel-condition-core.js','blacklight-exo-vessel-condition-propagation.js','blacklight-exo-vessel-condition-voxel.js','blacklight-exo-vessel-condition-runtime.js','blacklight-exo-vessel-condition-contracts.js'
];
const fail=message=>{throw new Error(message);};
const finite=(value,label)=>{if(!Number.isFinite(Number(value)))fail(`${label} is not finite: ${value}`);return Number(value);};
const close=(a,b,tolerance,label)=>{const left=finite(a,`${label} left`),right=finite(b,`${label} right`),error=Math.abs(left-right)/Math.max(1,Math.abs(right));if(error>tolerance)fail(`${label} differs by ${(error*100).toFixed(9)}%.`);};
const clone=value=>structuredClone(value);
const storage=new Map();
globalThis.localStorage={getItem:key=>storage.has(key)?storage.get(key):null,setItem:(key,value)=>storage.set(key,String(value)),removeItem:key=>storage.delete(key),clear:()=>storage.clear()};
globalThis.document={getElementById:()=>null,querySelector:()=>null,querySelectorAll:()=>[]};
for(const filename of files){const source=await fs.readFile(path.join(root,filename),'utf8');new vm.Script(source,{filename}).runInThisContext();}

const vessel=globalThis.BlacklightExoVessel,definitions=globalThis.BlacklightExoVesselConditionDefinitions,propagation=globalThis.BlacklightExoVesselConditionPropagation,contracts=globalThis.BlacklightExoVesselContracts;
if(!vessel||!definitions||!propagation||!contracts)fail('VESSEL-05 condition stack did not initialize.');
if(vessel.version!==3||vessel.conditionHistoryVersion!==1||vessel.conditionContractVersion!==1||vessel.voxelContractVersion!==1||vessel.moduleHardeningVersion!==1||vessel.distributedArmorVersion!==1||propagation.voxelConditionVersion!==1)fail('Unexpected VESSEL-05 wrapper stack.');
if(definitions.phase!=='VESSEL-05'||definitions.installationStates.length!==5||definitions.serviceStates.length!==7||definitions.eventTypes.length!==19||definitions.faultTypes.length!==6)fail('Condition definitions are incomplete.');

const registry=JSON.parse(await fs.readFile(path.join(root,'data/exo-vessel/condition-history-registry.json'),'utf8'));
const schema=JSON.parse(await fs.readFile(path.join(root,'data/schemas/exo-vessel-condition-history.schema.json'),'utf8'));
const conditionSchema=JSON.parse(await fs.readFile(path.join(root,'data/schemas/exo-vessel-condition.schema.json'),'utf8'));
const moduleSchema=JSON.parse(await fs.readFile(path.join(root,'data/schemas/exo-vessel-module.schema.json'),'utf8'));
if(registry.registryVersion!=='1.0.0'||registry.phase!=='VESSEL-05'||registry.schema!=='data/schemas/exo-vessel-condition-history.schema.json')fail('Condition-history registry identity is incorrect.');
if(registry.validationModes.join(',')!=='REPAIR,STRICT'||registry.repairableFaults.length!==6)fail('Condition-history registry validation policy is incomplete.');
if(schema.$schema!=='https://json-schema.org/draft/2020-12/schema'||schema.$id!=='https://mrcalzon02.github.io/HB-TTRPG-tools/data/schemas/exo-vessel-condition-history.schema.json')fail('Condition-history schema identity is incorrect.');
for(const key of ['recordType','schemaVersion','phase','vesselInstanceId','conditionSeed','historySeed','activeTemplate','axes','coherentVesselGraph','validationMode','events','moduleStates','voxelStates','residualVoxelField','zoneStates','routeStates','loadPathStates','effectiveGraphs','recalculated','repairLog','deferredSystems','validation'])if(!schema.required.includes(key))fail(`Condition-history schema does not require ${key}.`);
if(!conditionSchema.properties.applicationStatus.enum.includes('APPLIED_TO_MODULE_AND_VOXEL_GRAPHS'))fail('Condition schema does not permit graph-applied status.');
for(const key of ['installationState','serviceState','disposition','graphParticipation','residualMassTonnes','salvageableMassTonnes','failureModes','eventIds'])if(!moduleSchema.properties.state.required.includes(key))fail(`Module state schema does not require ${key}.`);

const templates=Object.keys(contracts.conditionTemplates);
const baseInput={family:'metric-envelope',pathLevel:'p4',role:'warship',biologyProfile:'human-standard',defense:'naval',manufacturerProfile:'APEX_WARLORD',manufacturerIndex:2,designEnvelope:'AUTO',combatFit:'NAVAL',graphValidationMode:'REPAIR',voxelValidationMode:'REPAIR',conditionValidationMode:'REPAIR',crew:42,enduranceDays:220,reserveJumps:3,distanceLy:4,payloadTonnes:''};
function stripped(record){const copy=clone(record);delete copy.generatedAt;delete copy.contract.createdAt;delete copy.contract.updatedAt;return copy;}
function checkResult(result,label){
  const history=result.conditionHistory;if(!history||history.recordType!=='exoVesselConditionHistory'||history.schemaVersion!=='1.0.0'||history.phase!=='VESSEL-05')fail(`${label} lacks canonical condition history.`);
  if(!history.validation.valid)fail(`${label} condition history invalid: ${history.validation.violations.join('; ')}`);
  if(!result.contract.validation.valid)fail(`${label} canonical contract invalid: ${result.contract.validation.violations.join('; ')}`);
  if(result.contract.provenance.generatorVersion!=='3.5.0'||result.contract.provenance.conditionHistoryVersion!=='1.0.0')fail(`${label} provenance does not identify VESSEL-05.`);
  if(result.contract.extensions.conditionHistorySchema!=='data/schemas/exo-vessel-condition-history.schema.json')fail(`${label} contract does not expose the condition-history schema.`);
  if(result.contract.condition.applicationStatus!=='APPLIED_TO_MODULE_AND_VOXEL_GRAPHS')fail(`${label} condition remains schema-only.`);
  if(result.contract.derivedLayers.find(layer=>layer.key==='damageTopology')?.status!=='generated')fail(`${label} did not generate damageTopology.`);
  for(const key of ['combatEnvelope','gameplayStatBlock','actionSet'])if(result.contract.derivedLayers.find(layer=>layer.key===key)?.status!=='planned')fail(`${label} prematurely generated ${key}.`);
  if(history.vesselInstanceId!==result.contract.identifiers.vesselInstanceId)fail(`${label} condition and contract vessel IDs disagree.`);
  if(history.moduleStates.length!==result.moduleGraph.modules.length)fail(`${label} condition inventory differs from module inventory.`);
  const stateMap=new Map(history.moduleStates.map(state=>[state.moduleId,state]));
  for(const module of result.moduleGraph.modules){const state=stateMap.get(module.moduleId);if(!state)fail(`${label}/${module.moduleId} lacks condition state.`);const{moduleId,...expected}=state;if(JSON.stringify(module.state)!==JSON.stringify(expected))fail(`${label}/${module.moduleId} module state diverges from history.`);if(state.installationState==='MISSING'&&(state.damagePercent||state.salvageRemovalPercent))fail(`${label}/${module.moduleId} confuses missing construction with damage or salvage.`);if(state.disposition==='TEARDOWN'&&state.damagePercent)fail(`${label}/${module.moduleId} confuses teardown with damage.`);if(state.disposition==='SALVAGE'&&!state.salvageRemovalPercent)fail(`${label}/${module.moduleId} lacks salvage provenance.`);if(state.installationState==='REMOVED'&&state.graphParticipation!=='NONE')fail(`${label}/${module.moduleId} removed hardware still participates in graphs.`);if(state.salvageableMassTonnes>state.residualMassTonnes+1e-9)fail(`${label}/${module.moduleId} salvage exceeds residual material.`);}
  close(history.recalculated.residualMassTonnes,history.moduleStates.reduce((sum,state)=>sum+state.residualMassTonnes,0),1e-10,`${label} residual mass closure`);
  close(history.recalculated.salvageableMassTonnes,history.moduleStates.reduce((sum,state)=>sum+state.salvageableMassTonnes,0),1e-10,`${label} salvage mass closure`);
  if(history.recalculated.salvageableMassTonnes>history.recalculated.residualMassTonnes+1e-9)fail(`${label} aggregate salvage exceeds residual material.`);
  const placements=result.voxelLayout.modulePlacements,voxelStates=history.voxelStates,placementIds=new Set(placements.map(item=>item.placementId)),voxelStateIds=new Set(voxelStates.map(item=>item.placementId));
  if(placementIds.size!==voxelStateIds.size||[...placementIds].some(id=>!voxelStateIds.has(id)))fail(`${label} voxel condition inventory differs from intact placement inventory.`);
  for(const state of voxelStates){if(['MISSING','REMOVED'].includes(state.installationState)&&state.occupied)fail(`${label}/${state.placementId} is absent but occupied.`);if(state.residualVolumeM3===0&&state.occupied)fail(`${label}/${state.placementId} has no residual volume but is occupied.`);if(state.graphParticipation==='NONE'&&state.coherentParticipation)fail(`${label}/${state.placementId} claims coherent participation without graph participation.`);}
  const field=history.residualVoxelField,occupied=voxelStates.filter(state=>state.occupied),active=voxelStates.filter(state=>state.graphParticipation==='ACTIVE'),wreckage=voxelStates.filter(state=>['WRECKAGE','DESTROYED_WRECKAGE','DAMAGED'].includes(state.conditionState));
  if(field.referencePlacementCount!==placements.length||field.occupiedPlacementCount!==occupied.length||field.activePlacementCount!==active.length||field.wreckagePlacementCount!==wreckage.length||field.absentPlacementCount!==voxelStates.length-occupied.length)fail(`${label} residual voxel field counts do not close.`);
  if(result.damageTopology.voxelStates.length!==voxelStates.length||result.damageTopology.residualVoxelField.referenceAuthority!=='VESSEL-04_INTACT_VOXEL_LAYOUT')fail(`${label} damage topology lacks voxel condition authority.`);
  for(const[name,graph]of Object.entries(history.effectiveGraphs)){if(graph.graphType!==name)fail(`${label}/${name} effective graph type mismatch.`);const nodes=new Set(graph.nodes);for(const edge of graph.edges)if(!nodes.has(edge.from)||!nodes.has(edge.to))fail(`${label}/${name}/${edge.edgeId} has missing effective endpoints.`);}
  if(history.axes.destructionPercent===100){if(history.coherentVesselGraph||field.coherentVesselGraph)fail(`${label} total destruction retained coherence.`);if(field.activePlacementCount!==0)fail(`${label} total destruction retained active voxel placements.`);if(Object.values(history.effectiveGraphs).some(graph=>graph.nodes.length||graph.edges.length))fail(`${label} total destruction retained effective topology.`);}
  if(history.axes.destructionPercent>=75&&history.axes.destructionPercent<100&&!history.coherentVesselGraph)fail(`${label} wreck lost coherent authority below total destruction.`);
  if(JSON.stringify(history).match(/lightLagSeconds|engagementEnvelope|hitProbability|impactVoxel/))fail(`${label} contains deferred combat or hit-resolution fields.`);
}

const results={};
for(const template of templates){const result=vessel.generate(`condition-template:${template}`,{...baseInput,conditionTemplate:template},null);checkResult(result,template);results[template]=result;const replay=vessel.generate(`condition-template:${template}`,{...baseInput,conditionTemplate:template},null);if(JSON.stringify(stripped(result))!==JSON.stringify(stripped(replay)))fail(`${template} is not deterministic outside timestamps.`);}
if(!results.OPERATIONAL.conditionHistory.moduleStates.every(state=>state.installationState==='INSTALLED'&&state.damagePercent===0&&state.salvageRemovalPercent===0))fail('OPERATIONAL is not an intact installed reference application.');
if(!results.PARTIALLY_COMPLETED.conditionHistory.moduleStates.some(state=>['MISSING','INCOMPLETE'].includes(state.installationState)))fail('PARTIALLY_COMPLETED produced no missing or incomplete construction.');
if(results.PARTIALLY_COMPLETED.conditionHistory.moduleStates.some(state=>state.damagePercent>0||state.salvageRemovalPercent>0))fail('PARTIALLY_COMPLETED confused construction shortfall with damage or salvage.');
if(!results.PARTIALLY_COMPLETED.conditionHistory.voxelStates.some(state=>['MISSING_CONSTRUCTION','INCOMPLETE'].includes(state.conditionState)))fail('PARTIALLY_COMPLETED did not apply construction state to voxel placements.');
if(!results.WORN_SERVICE.conditionHistory.events.some(event=>event.eventType==='SERVICE_WEAR'))fail('WORN_SERVICE lacks explicit service-wear history.');
if(!results.PARTIALLY_TORN_DOWN.conditionHistory.moduleStates.some(state=>state.disposition==='TEARDOWN'&&state.installationState==='REMOVED'))fail('PARTIALLY_TORN_DOWN produced no deliberate removals.');
if(results.PARTIALLY_TORN_DOWN.conditionHistory.moduleStates.some(state=>state.disposition==='TEARDOWN'&&state.damagePercent))fail('PARTIALLY_TORN_DOWN represented teardown as damage.');
if(!results.MOTHBALLED.conditionHistory.moduleStates.filter(state=>state.graphParticipation!=='NONE').every(state=>state.serviceState==='MOTHBALLED'&&!state.operational))fail('MOTHBALLED hardware was not preserved offline.');
if(!results.ABANDONED.conditionHistory.moduleStates.filter(state=>state.graphParticipation!=='NONE').every(state=>state.serviceState==='ABANDONED'&&!state.operational))fail('ABANDONED hardware retained ordinary crew operation.');
if(!results.PARTIALLY_SALVAGED.conditionHistory.moduleStates.some(state=>state.disposition==='SALVAGE'&&state.salvageRemovalPercent===100))fail('PARTIALLY_SALVAGED produced no salvage removal provenance.');
if(!results.DAMAGED.conditionHistory.moduleStates.some(state=>state.damagePercent>0&&state.installationState!=='REMOVED'))fail('DAMAGED produced no damaged installed hardware.');
if(!results.WRECKED.conditionHistory.coherentVesselGraph)fail('WRECKED did not retain coherent wreckage.');
if(results.DESTROYED.conditionHistory.coherentVesselGraph||results.DESTROYED.conditionHistory.recalculated.readinessScore!==0)fail('DESTROYED retained coherence or readiness.');
const seventyFive=vessel.generate('condition:seventy-five',{...baseInput,conditionTemplate:'DAMAGED',conditionOverrides:{destructionPercent:75,structuralDamagePercent:75,systemDamagePercent:75,operationalReadinessPercent:0}},null);checkResult(seventyFive,'75% destruction');if(!seventyFive.conditionHistory.coherentVesselGraph)fail('75% destruction did not retain coherent wreckage.');

const faultCases={DESTROYED_WITH_COHERENT_GRAPH:'DESTROYED',REMOVED_MODULE_STILL_PARTICIPATING:'PARTIALLY_TORN_DOWN',SALVAGE_EXCEEDS_MASS:'PARTIALLY_SALVAGED',MISSING_EVENT_TARGET:'DAMAGED',OPERATIONAL_WITH_ZERO_CONSTRUCTION:'OPERATIONAL',BROKEN_SURVIVING_LOAD_PATH:'OPERATIONAL'};
for(const[fault,template]of Object.entries(faultCases)){const repaired=vessel.generate(`condition-repair:${fault}`,{...baseInput,conditionTemplate:template,conditionFault:fault,conditionValidationMode:'REPAIR'},null);checkResult(repaired,`repair/${fault}`);if(!repaired.conditionHistory.validation.preRepairViolations?.length)fail(`${fault} repair did not retain pre-repair violations.`);if(!repaired.conditionHistory.repairLog.length)fail(`${fault} repair did not retain a deterministic repair record.`);let rejected=false;try{vessel.generate(`condition-strict:${fault}`,{...baseInput,conditionTemplate:template,conditionFault:fault,conditionValidationMode:'STRICT'},null);}catch{rejected=true;}if(!rejected)fail(`${fault} was not rejected in STRICT mode.`);}

const families=globalThis.BlacklightExoFTL.families||[],levels=globalThis.BlacklightExoFTL.pathLevels||[],archetypes=['VAULT_KEEPER','VOID_NOMAD','CORP_LOGISTICS','APEX_WARLORD'];let cases=0;
for(const family of families)for(const level of levels)for(let manufacturerIndex=0;manufacturerIndex<archetypes.length;manufacturerIndex+=1){const template=templates[cases%templates.length],input={...baseInput,family:family.key,pathLevel:level.key,role:vessel.roles[cases%vessel.roles.length].key,defense:vessel.defenses[cases%vessel.defenses.length].key,manufacturerProfile:archetypes[manufacturerIndex],manufacturerIndex,conditionTemplate:template,crew:10+(cases%71),enduranceDays:45+(cases%9)*60,reserveJumps:1+(cases%4),distanceLy:.5+(cases%12)*1.4},seed=`condition-matrix:${family.key}:${level.key}:${manufacturerIndex}`,result=vessel.generate(seed,input,null);checkResult(result,`${family.key}/${level.key}/house-${manufacturerIndex}/${template}`);cases+=1;}

const page=await fs.readFile(path.join(root,'blacklight-exo-vessel.html'),'utf8');
const loaderOrder=['blacklight-exo-vessel-voxel-contracts.js','blacklight-exo-vessel-condition-definitions.js','blacklight-exo-vessel-condition-core.js','blacklight-exo-vessel-condition-propagation.js','blacklight-exo-vessel-condition-voxel.js','blacklight-exo-vessel-condition-runtime.js','blacklight-exo-vessel-condition-contracts.js','blacklight-exo-vessel-ui.js','blacklight-exo-vessel-voxel-ui.js','blacklight-exo-vessel-condition-ui.js','blacklight-exo-vessel-contract-ui.js'];let prior=-1;for(const marker of loaderOrder){const index=page.indexOf(marker);if(index<0)fail(`Vessel page does not load ${marker}.`);if(index<=prior)fail(`Vessel page loads ${marker} out of order.`);prior=index;}
const ui=await fs.readFile(path.join(root,'blacklight-exo-vessel-condition-ui.js'),'utf8');for(const marker of ['exo-vessel-condition-mode','exo-vessel-export-condition-history','exo-vessel-condition-history-section','exo-vessel-condition-module-body','exo-vessel-condition-event-body','exo-vessel-condition-zone-body','exo-vessel-condition-graph-body','exo-vessel-condition-repair-log'])if(!ui.includes(marker))fail(`Condition UI lacks ${marker}.`);

console.log('EXO vessel VESSEL-05 condition, history, salvage, wreck, and destruction validation passed.');
console.log(`Validated ${templates.length} lifecycle templates, ${cases} family/Path/manufacturer cases, deterministic replay, six repair and strict-rejection faults, distinct missing/removal/salvage/damage states, module and voxel condition application, residual voxel-field closure, surviving graph recalculation, 75% coherent wreckage, 100% topology removal, schema authority, UI markers, and loader order.`);

import fs from 'node:fs/promises';
import path from 'node:path';
import process from 'node:process';
import vm from 'node:vm';

const root=process.cwd();
const files=[
  'blacklight-exo-ftl-physics-definitions.js','blacklight-exo-ftl-operational-definitions.js','blacklight-exo-ftl-runtime.js','blacklight-exo-ftl-core.js','blacklight-exo-ftl-engineering-extension.js',
  'blacklight-exo-ftl-path-level-core.js','blacklight-exo-ftl-path-level-paths-physical.js','blacklight-exo-ftl-path-level-paths-dimensional.js','blacklight-exo-ftl-path-level-paths-discrete.js','blacklight-exo-ftl-path-level-runtime.js','blacklight-exo-ftl-path-level-engineering.js','blacklight-exo-ftl-path-level-controller.js',
  'blacklight-exo-ftl-mechanism-core.js','blacklight-exo-ftl-mechanism-path-inertial-torch.js','blacklight-exo-ftl-mechanism-path-metric-envelope.js','blacklight-exo-ftl-mechanism-path-gravitic-plane.js','blacklight-exo-ftl-mechanism-path-slipstream-shear.js','blacklight-exo-ftl-mechanism-path-q-lattice.js','blacklight-exo-ftl-mechanism-path-n-manifold.js','blacklight-exo-ftl-mechanism-path-fold-jump.js','blacklight-exo-ftl-mechanism-path-wormhole-gate.js','blacklight-exo-ftl-mechanism-path-phase-displacement.js','blacklight-exo-ftl-mechanism-runtime.js',
  'blacklight-exo-ftl-assembly-core.js','blacklight-exo-ftl-assembly-runtime.js','blacklight-exo-ftl-calculation-core.js','blacklight-exo-ftl-calculation-performance.js','blacklight-exo-ftl-calculation-energy.js','blacklight-exo-ftl-calculation-runtime.js','blacklight-exo-ftl-certification-core.js','blacklight-exo-ftl-certification-route.js','blacklight-exo-ftl-certification-reliability.js','blacklight-exo-ftl-certification-runtime.js',
  'blacklight-exo-vessel-definitions.js','blacklight-exo-vessel-biology.js','blacklight-exo-vessel-core.js','blacklight-exo-vessel-philosophy-definitions.js','blacklight-exo-vessel-manufacturer-definitions.js','blacklight-exo-vessel-manufacturer-runtime.js','blacklight-exo-vessel-philosophy-runtime.js','blacklight-exo-vessel-engineering-definitions.js','blacklight-exo-vessel-engineering-runtime.js','blacklight-exo-vessel-armor-distribution.js','blacklight-exo-vessel-module-definitions.js','blacklight-exo-vessel-voxel-definitions.js','blacklight-exo-vessel-voxel-core.js','blacklight-exo-vessel-voxel-routing.js','blacklight-exo-vessel-contracts.js','blacklight-exo-vessel-module-runtime.js','blacklight-exo-vessel-module-hardening.js','blacklight-exo-vessel-voxel-runtime.js','blacklight-exo-vessel-voxel-armor.js','blacklight-exo-vessel-voxel-contracts.js'
];
const fail=message=>{throw new Error(message);};
const finite=(value,label)=>{if(!Number.isFinite(Number(value)))fail(`${label} is not finite: ${value}`);return Number(value);};
const close=(a,b,tolerance,label)=>{const left=finite(a,`${label} left`),right=finite(b,`${label} right`),error=Math.abs(left-right)/Math.max(1,Math.abs(right));if(error>tolerance)fail(`${label} differs by ${(error*100).toFixed(9)}%.`);};
const clone=value=>structuredClone(value);
const pointInside=(point,grid)=>point.x>=0&&point.y>=0&&point.z>=0&&point.x<=grid.size.x-1&&point.y<=grid.size.y-1&&point.z<=grid.size.z-1;
const overlaps=(a,b)=>a.min.x<=b.max.x&&a.max.x>=b.min.x&&a.min.y<=b.max.y&&a.max.y>=b.min.y&&a.min.z<=b.max.z&&a.max.z>=b.min.z;
const stripTimes=value=>{const copy=clone(value);delete copy.generatedAt;if(copy.contract){delete copy.contract.createdAt;delete copy.contract.updatedAt;}return copy;};
globalThis.localStorage={getItem:()=>null,setItem:()=>{},removeItem:()=>{},clear:()=>{}};globalThis.document={getElementById:()=>null,querySelector:()=>null,querySelectorAll:()=>[]};
for(const filename of files){const source=await fs.readFile(path.join(root,filename),'utf8');new vm.Script(source,{filename}).runInThisContext();}

const vessel=globalThis.BlacklightExoVessel,definitions=globalThis.BlacklightExoVesselVoxelDefinitions;
if(!vessel||!definitions)fail('VESSEL-04 voxel assembler runtime did not initialize.');
if(vessel.voxelLayoutVersion!==1||vessel.voxelArmorVersion!==1||vessel.voxelContractVersion!==1||vessel.moduleHardeningVersion!==1||vessel.distributedArmorVersion!==1)fail('Unexpected distributed-armor voxel stack.');
if(Object.keys(definitions.topologyPolicies).sort().join(',')!==['CLUSTER','HYBRID','MONOCOQUE','RING','SPINE'].join(','))fail('Voxel definitions do not expose all five topology algorithms.');
const registry=JSON.parse(await fs.readFile(path.join(root,'data/exo-vessel/voxel-layout-registry.json'),'utf8'));if(registry.phase!=='VESSEL-04'||registry.maxEnvelopeCells!==120000)fail('Voxel registry identity is incorrect.');

const source={type:'biology',dossier:{version:3,seed:'vesper-voxel-culture',generatedAt:'2026-07-17T00:00:00.000Z',system:{name:'Vesper Array',state:'Dense interstellar hub system',stateKey:'hub',development:91,life:'living',economy:'interstellar transit-service economy',traffic:'continuous interstellar traffic'},species:{name:'Vesper Assemblies',commonName:'Vesper',environment:'high-gravity artificial habitat network',bodyPlan:'distributed colonial organism',chemistry:'engineered synthetic biochemistry',senses:['broad-spectrum vision','electromagnetic field sensing'],cognition:'machine-mediated collective memory',communication:'shared augmented-reality glyphs',reproduction:'manufactured gestation',lifespan:'220 local years',size:'2.1 m typical adult span',adaptation:'radiation-repair enzymes',extinct:false},civilization:{status:'active',government:'bureaucratic republic',economy:'interstellar transit-service economy',technology:'Advanced interstellar',reach:'Distributed interstellar network',values:['precision and proof','collective survival','commercial reputation'],law:'algorithmic regulation',warfare:'professional expeditionary fleets'}}};
const baseInput={family:'metric-envelope',pathLevel:'p4',role:'warship',biologyProfile:'inherit',defense:'naval',manufacturerProfile:'APEX_WARLORD',manufacturerIndex:2,designEnvelope:'AUTO',combatFit:'NAVAL',graphValidationMode:'REPAIR',voxelValidationMode:'REPAIR',crew:42,enduranceDays:220,reserveJumps:3,distanceLy:4,payloadTonnes:'',conditionTemplate:'OPERATIONAL'};
const directionKeys=['FORE','AFT','LEFT','RIGHT','UP','DOWN','CITADEL','STRUCTURAL'];

function checkResult(result,label){
  const graph=result.moduleGraph,layout=result.voxelLayout,modules=graph?.modules||[];
  if(!layout?.validation?.valid)fail(`${label} lacks a valid voxel layout: ${layout?.validation?.violations?.join('; ')}`);
  if(!graph?.validation?.valid||!result.contract?.validation?.valid)fail(`${label} lost graph or contract validity.`);
  if(result.contract.provenance.generatorVersion!=='3.4.0')fail(`${label} contract provenance does not identify VESSEL-04.`);
  if(layout.vesselInstanceId!==graph.vesselInstanceId||layout.vesselInstanceId!==result.contract.identifiers.vesselInstanceId)fail(`${label} vessel IDs disagree.`);
  if(layout.topology.key!==graph.topology.key)fail(`${label} topology authority diverged.`);
  if(layout.grid.envelopeCellCount>definitions.maxEnvelopeCells||layout.grid.envelopeCellCount!==layout.grid.size.x*layout.grid.size.y*layout.grid.size.z)fail(`${label} violates voxel envelope policy.`);
  if(layout.grid.physicalSizeM.x+1e-9<result.hull.lengthM||layout.grid.physicalSizeM.y+1e-9<result.hull.beamM||layout.grid.physicalSizeM.z+1e-9<result.hull.heightM)fail(`${label} voxel envelope is smaller than the hull.`);
  if(layout.modulePlacements.length!==modules.length||layout.occupiedBlocks.length!==modules.length)fail(`${label} lacks one placement per module.`);
  if(layout.modulePlacements.some(item=>item.semanticType==='ARMOR')||modules.some(item=>item.semanticType==='ARMOR'))fail(`${label} contains obsolete armor-module geometry.`);
  const moduleIds=new Set(modules.map(module=>module.moduleId)),placementIds=new Set();
  for(const placement of layout.modulePlacements){if(placementIds.has(placement.moduleId)||!moduleIds.has(placement.moduleId))fail(`${label} has invalid placement identity.`);placementIds.add(placement.moduleId);if(!pointInside(placement.bounds.min,layout.grid)||!pointInside(placement.bounds.max,layout.grid))fail(`${label}/${placement.moduleId} leaves the grid.`);if(placement.capacityCells<placement.requiredCells||placement.representedVolumeM3+1e-9<placement.actualModuleVolumeM3)fail(`${label}/${placement.moduleId} does not fit.`);const module=modules.find(item=>item.moduleId===placement.moduleId);if(JSON.stringify(module.voxelBounds)!==JSON.stringify(placement.bounds))fail(`${label}/${placement.moduleId} lost canonical bounds.`);}
  for(let i=0;i<layout.modulePlacements.length;i+=1)for(let j=i+1;j<layout.modulePlacements.length;j+=1)if(overlaps(layout.modulePlacements[i].bounds,layout.modulePlacements[j].bounds))fail(`${label} has overlapping modules.`);
  const validNodes=new Set([...graph.infrastructureNodes.map(node=>node.nodeId),...moduleIds]);for(const [name,routes]of Object.entries(layout.utilityRoutes)){if(routes.length!==graph.graphs[name].edges.length)fail(`${label}/${name} route count diverges.`);for(const route of routes)if(!validNodes.has(route.fromNodeId)||!validNodes.has(route.toNodeId)||route.points.some(point=>!pointInside(point,layout.grid)))fail(`${label}/${route.routeId} is invalid.`);}
  const engines=layout.modulePlacements.filter(item=>item.semanticType==='MAIN_ENGINE');if(new Set(engines.map(item=>item.bounds.topologyLane)).size!==engines.length)fail(`${label} engines share an aft lane.`);for(const clearance of layout.engineExhaustClearances)if(!clearance.clear||clearance.points.some(point=>!pointInside(point,layout.grid)))fail(`${label} has blocked exhaust.`);for(const clearance of layout.radiatorClearances)if(!clearance.clear||clearance.points.some(point=>!pointInside(point,layout.grid)))fail(`${label} has blocked radiator clearance.`);for(const hardpoint of layout.weaponHardpointPlacements)if(!hardpoint.valid||hardpoint.selfOccluded)fail(`${label} has self-occluded weapons.`);
  if(layout.armorSurfaces.length!==8)fail(`${label} does not expose eight distributed protection surfaces.`);const facings=new Set(layout.armorSurfaces.map(surface=>surface.facing));if(directionKeys.some(key=>!facings.has(key)))fail(`${label} lacks a required armor or shielding direction.`);for(const surface of layout.armorSurfaces){if(surface.moduleId!==null||surface.standaloneModule!==false||surface.source!=='VESSEL_WIDE_DISTRIBUTED_ARMOR_LEDGER')fail(`${label}/${surface.facing} reverted to module armor.`);if(surface.passive.massTonnes<0||surface.activeField.massTonnes<0||surface.passive.equivalentThicknessMm<0)fail(`${label}/${surface.facing} has invalid protection values.`);}
  close(layout.armorSurfaces.reduce((sum,surface)=>sum+surface.passive.massTonnes,0),result.armor.passiveArmorMassTonnes,1e-10,`${label} passive surface mass`);close(layout.armorSurfaces.reduce((sum,surface)=>sum+surface.activeField.massTonnes,0),result.armor.fieldProtectionMassTonnes,1e-10,`${label} active field surface mass`);
  close(graph.hardeningSummary.distributedMassTonnes,result.armor.passiveArmorMassTonnes,1e-10,`${label} graph hardening summary`);
  if(layout.deferredSystems.conditionApplication!=='VESSEL-05'||layout.deferredSystems.trackAndCombatGeometry!=='VESSEL-06'||layout.deferredSystems.weaponEngagementEnvelopes!=='VESSEL-07'||layout.deferredSystems.localDamageResolution!=='VESSEL-08')fail(`${label} violated phased boundaries.`);
}

const reference=vessel.generate('voxel-reference',baseInput,source);checkResult(reference,'reference vessel');const replay=vessel.generate('voxel-reference',baseInput,source);if(JSON.stringify(stripTimes(reference))!==JSON.stringify(stripTimes(replay)))fail('Voxel generation is not deterministic outside timestamps.');
const repaired=vessel.generate('voxel-repair',{...baseInput,voxelLayoutFaults:['OVERLAP_FIRST_TWO','REMOVE_FIRST_PLACEMENT','BLOCK_FIRST_EXHAUST','BLOCK_FIRST_RADIATOR','OCCLUDE_FIRST_WEAPON'],voxelValidationMode:'REPAIR'},source);checkResult(repaired,'repaired vessel');if(repaired.voxelLayout.repairLog.length!==5)fail('Voxel repair did not retain all injected faults.');let strictRejected=false;try{vessel.generate('voxel-strict',{...baseInput,voxelLayoutFaults:['OVERLAP_FIRST_TWO'],voxelValidationMode:'STRICT'},source);}catch(error){strictRejected=/Voxel layout rejected/.test(String(error.message));}if(!strictRejected)fail('STRICT voxel validation did not reject overlap.');let forcedRejected=false;try{vessel.generate('voxel-forced-too-fine',{...baseInput,voxelCellEdgeM:.001},source);}catch(error){forcedRejected=/maximum envelope cell budget/.test(String(error.message));}if(!forcedRejected)fail('Excessively fine forced resolution was not rejected.');
const unarmed=vessel.generate('voxel-unarmed',{...baseInput,combatFit:'UNARMED'},source);checkResult(unarmed,'unarmed vessel');if(unarmed.voxelLayout.weaponHardpointPlacements.length)fail('UNARMED vessel retained weapon geometry.');

const families=globalThis.BlacklightExoFTL.families||[],levels=globalThis.BlacklightExoFTL.pathLevels||[],archetypes=['VAULT_KEEPER','VOID_NOMAD','CORP_LOGISTICS','APEX_WARLORD'],fits=['UNARMED','CIVILIAN','DEFENSIVE','SECURITY','NAVAL'],observedTopologies=new Set();let cases=0;
for(const family of families)for(const level of levels)for(let manufacturerIndex=0;manufacturerIndex<4;manufacturerIndex+=1){const role=vessel.roles[(cases+manufacturerIndex)%vessel.roles.length],fit=fits[(cases+manufacturerIndex)%fits.length],input={family:family.key,pathLevel:level.key,role:role.key,biologyProfile:'inherit',defense:vessel.defenses[cases%vessel.defenses.length].key,manufacturerProfile:archetypes[manufacturerIndex],manufacturerIndex,designEnvelope:'AUTO',combatFit:fit,graphValidationMode:cases%2?'STRICT':'REPAIR',voxelValidationMode:cases%2?'REPAIR':'STRICT',crew:10+(cases%83),enduranceDays:30+(cases%12)*45,reserveJumps:1+(cases%4),distanceLy:.25+(cases%14)*1.25,payloadTonnes:'',conditionTemplate:'OPERATIONAL'},seed=`voxel-matrix:${family.key}:${level.key}:${manufacturerIndex}`,result=vessel.generate(seed,input,source),again=vessel.generate(seed,input,source);cases+=1;checkResult(result,`${family.key}/${level.key}/house-${manufacturerIndex}`);observedTopologies.add(result.voxelLayout.topology.key);if(JSON.stringify(stripTimes(result))!==JSON.stringify(stripTimes(again)))fail(`${family.key}/${level.key}/house-${manufacturerIndex} is not deterministic.`);}
if(observedTopologies.size!==5)fail(`Matrix did not exercise all five topologies: ${[...observedTopologies].join(', ')}.`);
const page=await fs.readFile(path.join(root,'blacklight-exo-vessel.html'),'utf8');const order=['blacklight-exo-vessel-armor-distribution.js','blacklight-exo-vessel-module-runtime.js','blacklight-exo-vessel-module-hardening.js','blacklight-exo-vessel-voxel-runtime.js','blacklight-exo-vessel-voxel-armor.js','blacklight-exo-vessel-voxel-contracts.js'];let prior=-1;for(const marker of order){const index=page.indexOf(marker);if(index<0||index<=prior)fail(`Vessel page does not load ${marker} in order.`);prior=index;}
console.log('EXO vessel VESSEL-04 voxel assembler validation passed.');
console.log(`Validated ${cases} family/Path/manufacturer cases, all five topologies, non-overlapping module placement, routing and clearances, zero armor modules, six hull directions, citadel and structural protection, active-field overlays, deterministic repair, strict rejection, and loader order.`);

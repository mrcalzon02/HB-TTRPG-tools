import fs from 'node:fs/promises';
import path from 'node:path';
import process from 'node:process';
import vm from 'node:vm';

const root=process.cwd();
const files=[
  'blacklight-exo-ftl-physics-definitions.js','blacklight-exo-ftl-operational-definitions.js','blacklight-exo-ftl-runtime.js','blacklight-exo-ftl-core.js','blacklight-exo-ftl-engineering-extension.js',
  'blacklight-exo-ftl-path-level-core.js','blacklight-exo-ftl-path-level-paths-physical.js','blacklight-exo-ftl-path-level-paths-dimensional.js','blacklight-exo-ftl-path-level-paths-discrete.js','blacklight-exo-ftl-path-level-runtime.js','blacklight-exo-ftl-path-level-engineering.js','blacklight-exo-ftl-path-level-controller.js',
  'blacklight-exo-ftl-mechanism-core.js','blacklight-exo-ftl-mechanism-path-inertial-torch.js','blacklight-exo-ftl-mechanism-path-metric-envelope.js','blacklight-exo-ftl-mechanism-path-gravitic-plane.js','blacklight-exo-ftl-mechanism-path-slipstream-shear.js','blacklight-exo-ftl-mechanism-path-q-lattice.js','blacklight-exo-ftl-mechanism-path-n-manifold.js','blacklight-exo-ftl-mechanism-path-fold-jump.js','blacklight-exo-ftl-mechanism-path-wormhole-gate.js','blacklight-exo-ftl-mechanism-path-phase-displacement.js','blacklight-exo-ftl-mechanism-runtime.js',
  'blacklight-exo-ftl-assembly-core.js','blacklight-exo-ftl-assembly-runtime.js','blacklight-exo-ftl-calculation-core.js','blacklight-exo-ftl-calculation-performance.js','blacklight-exo-ftl-calculation-energy.js','blacklight-exo-ftl-calculation-runtime.js','blacklight-exo-ftl-certification-core.js','blacklight-exo-ftl-certification-route.js','blacklight-exo-ftl-certification-reliability.js','blacklight-exo-ftl-certification-runtime.js',
  'blacklight-exo-vessel-definitions.js','blacklight-exo-vessel-biology.js','blacklight-exo-vessel-core.js','blacklight-exo-vessel-philosophy-definitions.js','blacklight-exo-vessel-manufacturer-definitions.js','blacklight-exo-vessel-manufacturer-runtime.js','blacklight-exo-vessel-philosophy-runtime.js','blacklight-exo-vessel-engineering-definitions.js','blacklight-exo-vessel-engineering-runtime.js','blacklight-exo-vessel-armor-distribution.js','blacklight-exo-vessel-module-definitions.js','blacklight-exo-vessel-contracts.js','blacklight-exo-vessel-module-runtime.js','blacklight-exo-vessel-module-hardening.js'
];
const fail=message=>{throw new Error(message);};
const finite=(value,label)=>{if(!Number.isFinite(Number(value)))fail(`${label} is not finite: ${value}`);return Number(value);};
const close=(a,b,tolerance,label)=>{const left=finite(a,`${label} left`),right=finite(b,`${label} right`),error=Math.abs(left-right)/Math.max(1,Math.abs(right));if(error>tolerance)fail(`${label} differs by ${(error*100).toFixed(9)}%.`);};
const clone=value=>structuredClone(value);
globalThis.localStorage={getItem:()=>null,setItem:()=>{},removeItem:()=>{},clear:()=>{}};globalThis.document={getElementById:()=>null,querySelector:()=>null,querySelectorAll:()=>[]};
for(const filename of files){const source=await fs.readFile(path.join(root,filename),'utf8');new vm.Script(source,{filename}).runInThisContext();}

const vessel=globalThis.BlacklightExoVessel,definitions=globalThis.BlacklightExoVesselModuleDefinitions;
if(!vessel||!definitions)fail('VESSEL-03 semantic module graph runtime did not initialize.');
if(vessel.version!==3||vessel.moduleGraphVersion!==1||vessel.moduleHardeningVersion!==1||vessel.distributedArmorVersion!==1||vessel.engineeringLedgerVersion!==1||vessel.contractVersion!==1)fail('Unexpected distributed-hardening module stack.');
const graphSchema=JSON.parse(await fs.readFile(path.join(root,'data/schemas/exo-vessel-module-graph.schema.json'),'utf8'));if(!graphSchema.required.includes('hardeningSummary'))fail('Module graph schema does not require hardeningSummary.');

const source={type:'biology',dossier:{version:3,seed:'vesper-module-culture',generatedAt:'2026-07-17T00:00:00.000Z',system:{name:'Vesper Array',state:'Dense interstellar hub system',stateKey:'hub',development:91,life:'living',economy:'interstellar transit-service economy',traffic:'continuous interstellar traffic'},species:{name:'Vesper Assemblies',commonName:'Vesper',environment:'high-gravity artificial habitat network',bodyPlan:'distributed colonial organism',chemistry:'engineered synthetic biochemistry',senses:['broad-spectrum vision','electromagnetic field sensing'],cognition:'machine-mediated collective memory',communication:'shared augmented-reality glyphs',reproduction:'manufactured gestation',lifespan:'220 local years',size:'2.1 m typical adult span',adaptation:'radiation-repair enzymes',extinct:false},civilization:{status:'active',government:'bureaucratic republic',economy:'interstellar transit-service economy',technology:'Advanced interstellar',reach:'Distributed interstellar network',values:['precision and proof','collective survival','commercial reputation'],law:'algorithmic regulation',warfare:'professional expeditionary fleets'}}};
const baseInput={family:'metric-envelope',pathLevel:'p4',role:'warship',biologyProfile:'inherit',defense:'naval',manufacturerProfile:'APEX_WARLORD',manufacturerIndex:2,designEnvelope:'AUTO',combatFit:'NAVAL',graphValidationMode:'REPAIR',crew:42,enduranceDays:220,reserveJumps:3,distanceLy:4,payloadTonnes:'',conditionTemplate:'OPERATIONAL'};

function checkResult(result,label){
  const graph=result.moduleGraph,modules=graph?.modules||[];
  if(!graph||!graph.validation.valid)fail(`${label} lacks a valid semantic module graph.`);
  if(!result.contract?.validation?.valid)fail(`${label} contract invalid: ${result.contract?.validation?.violations?.join('; ')}`);
  if(result.contract.provenance.generatorVersion!=='3.3.0')fail(`${label} contract provenance does not identify VESSEL-03.`);
  if(modules.some(module=>module.semanticType==='ARMOR'||module.subsystemKey==='armor'))fail(`${label} generated a standalone armor module.`);
  if(result.hull.massBudget.some(row=>row.key==='armor'))fail(`${label} retained a standalone armor mass row.`);
  const summary=graph.hardeningSummary;if(!summary||summary.model!=='DISTRIBUTED_HULL_AND_SYSTEM_HARDENING'||summary.standaloneArmorModules!==false)fail(`${label} lacks the distributed hardening summary.`);
  close(summary.distributedMassTonnes,result.armor.passiveArmorMassTonnes,1e-10,`${label} graph hardening mass`);
  close(summary.distributedVolumeM3,result.hull.massBudget.reduce((sum,row)=>sum+finite(row.distributedHardeningVolumeM3),0),1e-10,`${label} graph hardening volume`);
  close(modules.reduce((sum,module)=>sum+finite(module.extensions?.hardening?.massTonnes),0),result.armor.passiveArmorMassTonnes,1e-10,`${label} module hardening mass`);
  const moduleIds=new Set(),nodeIds=new Set(graph.infrastructureNodes.map(node=>node.nodeId));
  for(const module of modules){
    if(moduleIds.has(module.moduleId))fail(`${label} duplicates ${module.moduleId}.`);moduleIds.add(module.moduleId);nodeIds.add(module.moduleId);
    if(!module.extensions?.hardening||module.extensions.hardening.standaloneModule!==false)fail(`${label}/${module.moduleId} lacks distributed hardening provenance.`);
    if(finite(module.extensions.hardening.massTonnes)>finite(module.massTonnes)+1e-9)fail(`${label}/${module.moduleId} hardening exceeds module mass.`);
    const parent=graph.infrastructureNodes.find(node=>node.nodeId===module.attachment.parentId);if(!parent||!parent.properties.includes(module.attachment.requiredProperty))fail(`${label}/${module.moduleId} has invalid attachment authority.`);
    if(module.envelope==='INTERNAL'&&(!module.pressureZoneId||module.attachment.requiredProperty!=='ATMOSPHERE_MANIFOLD'))fail(`${label}/${module.moduleId} violates internal protection rules.`);
    if(module.envelope==='EVA'&&(module.pressureZoneId!==null||module.attachment.requiredProperty!=='VACUUM_EXPOSED'))fail(`${label}/${module.moduleId} violates EVA rules.`);
    if(module.voxelBounds!==null)fail(`${label}/${module.moduleId} prematurely has voxel bounds.`);
  }
  close(modules.reduce((sum,module)=>sum+module.massTonnes,0),result.hull.totalMassTonnes,1e-10,`${label} module mass closure`);close(modules.reduce((sum,module)=>sum+module.volumeM3,0),result.hull.totalVolumeM3,1e-10,`${label} module volume closure`);
  for(const row of result.hull.massBudget){const derived=modules.filter(module=>module.provenance.sourceEngineeringKey===row.key);if(!derived.length)fail(`${label}/${row.key} produced no modules.`);close(derived.reduce((sum,module)=>sum+module.massTonnes,0),row.massTonnes,1e-10,`${label}/${row.key} mass`);}
  for(const [name,network]of Object.entries(graph.graphs))for(const edge of network.edges)if(!nodeIds.has(edge.from)||!nodeIds.has(edge.to))fail(`${label}/${name}/${edge.edgeId} references a missing node.`);
  for(const module of modules){if(!graph.graphs.structural.edges.some(edge=>edge.from===module.attachment.parentId&&edge.to===module.moduleId))fail(`${label}/${module.moduleId} lacks structural attachment.`);for(const [name,required]of Object.entries({power:module.requirements.power,cooling:module.requirements.cooling,data:module.requirements.data,atmosphere:module.requirements.atmosphere,access:module.requirements.access}))if(required&&!graph.graphs[name].edges.some(edge=>edge.to===module.moduleId))fail(`${label}/${module.moduleId} lacks ${name}.`);}
  for(const pathRecord of graph.loadPaths)if(!pathRecord.valid||!pathRecord.continuous||pathRecord.throughNodeIds.some(id=>!nodeIds.has(id)))fail(`${label}/${pathRecord.loadPathId} is not continuous.`);
  for(const hardpoint of graph.weaponHardpoints){if(!hardpoint.valid||!moduleIds.has(hardpoint.moduleId)||!hardpoint.sensorModuleIds.length||!hardpoint.fireControlModuleIds.length)fail(`${label}/${hardpoint.hardpointId} is invalid.`);}
  if(!result.manufacturer?.realizedModuleGraph)fail(`${label} lacks manufacturer graph realization.`);
}

const reference=vessel.generate('module-reference',baseInput,source);checkResult(reference,'reference vessel');const replay=vessel.generate('module-reference',baseInput,source);const left=clone(reference),right=clone(replay);delete left.generatedAt;delete right.generatedAt;delete left.contract.createdAt;delete right.contract.createdAt;delete left.contract.updatedAt;delete right.contract.updatedAt;if(JSON.stringify(left)!==JSON.stringify(right))fail('Module graph is not deterministic outside timestamps.');
const repaired=vessel.generate('module-repair',{...baseInput,moduleGraphFaults:['INVALID_FIRST_ATTACHMENT','REMOVE_FIRST_POWER_EDGE','REMOVE_FIRST_COOLING_EDGE','REMOVE_FIRST_DATA_EDGE','REMOVE_FIRST_ACCESS_EDGE','REMOVE_FIRST_MAGAZINE_LINK','BREAK_FIRST_LOAD_PATH'],graphValidationMode:'REPAIR'},source);checkResult(repaired,'repaired vessel');if(repaired.moduleGraph.repairLog.length<6)fail('Repair mode did not repair injected graph faults.');let strictRejected=false;try{vessel.generate('module-strict',{...baseInput,moduleGraphFaults:['INVALID_FIRST_ATTACHMENT'],graphValidationMode:'STRICT'},source);}catch(error){strictRejected=/Semantic module graph rejected/.test(String(error.message));}if(!strictRejected)fail('STRICT graph validation did not reject an invalid attachment.');
const unarmed=vessel.generate('module-unarmed',{...baseInput,combatFit:'UNARMED'},source);checkResult(unarmed,'unarmed vessel');if(unarmed.moduleGraph.weaponHardpoints.length||unarmed.moduleGraph.modules.some(module=>['WEAPON','WEAPON_SUPPORT','MAGAZINE','WEAPON_COOLING','COUNTERMEASURE'].includes(module.semanticType)))fail('UNARMED vessel retained combat modules.');

const families=globalThis.BlacklightExoFTL.families||[],levels=globalThis.BlacklightExoFTL.pathLevels||[],archetypes=['VAULT_KEEPER','VOID_NOMAD','CORP_LOGISTICS','APEX_WARLORD'],fits=['UNARMED','CIVILIAN','DEFENSIVE','SECURITY','NAVAL'];let cases=0;
for(const family of families)for(const level of levels)for(let manufacturerIndex=0;manufacturerIndex<4;manufacturerIndex+=1){const role=vessel.roles[(cases+manufacturerIndex)%vessel.roles.length],fit=fits[(cases+manufacturerIndex)%fits.length],input={family:family.key,pathLevel:level.key,role:role.key,biologyProfile:'inherit',defense:vessel.defenses[cases%vessel.defenses.length].key,manufacturerProfile:archetypes[manufacturerIndex],manufacturerIndex,designEnvelope:'AUTO',combatFit:fit,graphValidationMode:cases%2?'STRICT':'REPAIR',crew:10+(cases%83),enduranceDays:30+(cases%12)*45,reserveJumps:1+(cases%4),distanceLy:.25+(cases%14)*1.25,payloadTonnes:'',conditionTemplate:'OPERATIONAL'},seed=`module-matrix:${family.key}:${level.key}:${manufacturerIndex}`,result=vessel.generate(seed,input,source),again=vessel.generate(seed,input,source);cases+=1;checkResult(result,`${family.key}/${level.key}/house-${manufacturerIndex}`);const a=clone(result),b=clone(again);delete a.generatedAt;delete b.generatedAt;delete a.contract.createdAt;delete b.contract.createdAt;delete a.contract.updatedAt;delete b.contract.updatedAt;if(JSON.stringify(a)!==JSON.stringify(b))fail(`${family.key}/${level.key}/house-${manufacturerIndex} is not deterministic.`);}

const page=await fs.readFile(path.join(root,'blacklight-exo-vessel.html'),'utf8');const order=['blacklight-exo-vessel-armor-distribution.js','blacklight-exo-vessel-contracts.js','blacklight-exo-vessel-module-runtime.js','blacklight-exo-vessel-module-hardening.js','blacklight-exo-vessel-voxel-runtime.js'];let prior=-1;for(const marker of order){const index=page.indexOf(marker);if(index<0||index<=prior)fail(`Vessel page does not load ${marker} in order.`);prior=index;}
console.log('EXO vessel VESSEL-03 semantic module graph validation passed.');
console.log(`Validated ${cases} family/Path/manufacturer cases, zero standalone armor modules, exact distributed hardening provenance, module mass closure, attachments, pressure zones, utilities, load paths, weapons, deterministic repair, strict rejection, and loader order.`);

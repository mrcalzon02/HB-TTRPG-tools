(() => {
  'use strict';
  // Temporarily disabled. The previous subpanel implementation used broad mutation observers
  // that could destabilize the post-login route. Rebuild this as static markup or a narrowly
  // scoped one-shot initializer before re-enabling.
  window.BlacklightSystemsSubpanelDisabled = true;
})();

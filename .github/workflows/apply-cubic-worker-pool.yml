name: Apply deterministic Cubic worker pool

on:
  push:
    branches: ["main"]
    paths:
      - ".github/workflows/apply-cubic-worker-pool.yml"
      - "scripts/apply-cubic-worker-pool.py"
      - "binary-cube-cubic-decryptor-worker-pool.js"

permissions:
  contents: write
  statuses: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Record pending
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh api "repos/${{ github.repository }}/statuses/${{ github.sha }}" \
            -f state=pending \
            -f context='binary-cube/cubic-worker-pool' \
            -f description='Applying deterministic Cubic worker-pool sharding.' \
            -f target_url="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Apply exact worker-pool edits
        run: |
          git fetch origin main
          git checkout main
          git reset --hard origin/main
          python scripts/apply-cubic-worker-pool.py
          node --check binary-cube-cubic-decryptor-worker-pool.js
          node --check binary-cube-cubic-decryptor-worker.js
          node --check binary-cube-cubic-decryptor.js
          node --check scripts/validate-binary-cube-cubic-decryptor.mjs
          node --check scientific-tools-entry.js
          node --check app-lite-view-mounts.js
          node --check scripts/validate-scientific-tools-extraction.mjs
          node scripts/validate-binary-cube-cubic-decryptor.mjs
          node scripts/validate-scientific-diagnostic-pipeline.mjs
          node scripts/validate-scientific-tools-extraction.mjs
      - name: Commit validated worker pool
        run: |
          if git diff --quiet -- binary-cube-cubic-decryptor-worker.js binary-cube-cubic-decryptor.js scripts/validate-binary-cube-cubic-decryptor.mjs scientific-tools-entry.js app-lite-view-mounts.js scripts/validate-scientific-tools-extraction.mjs; then
            echo "Worker-pool integration already present."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add binary-cube-cubic-decryptor-worker.js binary-cube-cubic-decryptor.js scripts/validate-binary-cube-cubic-decryptor.mjs scientific-tools-entry.js app-lite-view-mounts.js scripts/validate-scientific-tools-extraction.mjs
          git commit -m "feat: shard Cubic decryptor search across deterministic workers"
          git push origin HEAD:main
      - name: Record success
        if: ${{ success() }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh api "repos/${{ github.repository }}/statuses/${{ github.sha }}" \
            -f state=success \
            -f context='binary-cube/cubic-worker-pool' \
            -f description='Deterministic Cubic worker pool integrated.' \
            -f target_url="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
      - name: Record failure
        if: ${{ failure() }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh api "repos/${{ github.repository }}/statuses/${{ github.sha }}" \
            -f state=failure \
            -f context='binary-cube/cubic-worker-pool' \
            -f description='Deterministic Cubic worker-pool migration failed closed.' \
            -f target_url="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"

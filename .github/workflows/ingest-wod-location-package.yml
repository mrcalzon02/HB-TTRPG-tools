name: Ingest World of Darkness Location Package

on:
  workflow_dispatch:
    inputs:
      issue_number:
        description: GitHub issue number containing the generated Chronicle location package
        required: true
        type: string

permissions:
  contents: write
  issues: read

concurrency:
  group: wod-location-package-registry
  cancel-in-progress: false

jobs:
  ingest:
    runs-on: ubuntu-latest
    steps:
      - name: Check out main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Download issue body
        env:
          GH_TOKEN: ${{ github.token }}
          ISSUE_NUMBER: ${{ inputs.issue_number }}
        run: gh issue view "$ISSUE_NUMBER" --repo "$GITHUB_REPOSITORY" --json body --jq .body > /tmp/wod-location-package-issue.txt

      - name: Validate and write generated package
        env:
          ISSUE_BODY_FILE: /tmp/wod-location-package-issue.txt
          ISSUE_NUMBER: ${{ inputs.issue_number }}
        run: node scripts/ingest-wod-location-package.mjs

      - name: Commit generated package registry
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/world-of-darkness/generated_location_registry.json
          if git diff --cached --quiet; then
            echo "No generated package changes to commit."
          else
            git commit -m "Publish World of Darkness location package #${{ inputs.issue_number }} [skip ci]"
            git pull --rebase origin main
            git push origin HEAD:main
          fi

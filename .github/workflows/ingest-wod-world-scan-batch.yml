name: Ingest World of Darkness World Scan Batch

on:
  workflow_dispatch:
    inputs:
      issue_number:
        description: GitHub issue containing a world-seeded named-location scan batch
        required: true
        type: string

permissions:
  contents: write
  issues: read

concurrency:
  group: wod-location-package-registry
  cancel-in-progress: false

jobs:
  ingest:
    runs-on: ubuntu-latest
    steps:
      - name: Check out main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Download issue body
        env:
          GH_TOKEN: ${{ github.token }}
          ISSUE_NUMBER: ${{ inputs.issue_number }}
        run: gh issue view "$ISSUE_NUMBER" --repo "$GITHUB_REPOSITORY" --json body --jq .body > /tmp/wod-world-scan-batch.txt

      - name: Validate and ingest named locations
        env:
          ISSUE_BODY_FILE: /tmp/wod-world-scan-batch.txt
          ISSUE_NUMBER: ${{ inputs.issue_number }}
        run: node scripts/ingest-wod-world-scan-batch.mjs

      - name: Commit global world scan
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/world-of-darkness/generated_location_registry.json
          if git diff --cached --quiet; then
            echo "No new global named locations were added."
          else
            git commit -m "Ingest World of Darkness world scan #${{ inputs.issue_number }} [skip ci]"
            git pull --rebase origin main
            git push origin HEAD:main
          fi

(()=>{'use strict';const THREE_URL='https://cdn.jsdelivr.net/npm/three@0.128.0/build/three.min.js';const ORBIT_URL='https://cdn.jsdelivr.net/npm/three@0.128.0/examples/js/controls/OrbitControls.js';const scriptLoads=new Map();const VALID_MODES=new Set(['select','orbit','pan','zoom']);function loadScript(src){if(scriptLoads.has(src))return scriptLoads.get(src);const resolved=new URL(src,document.baseURI).href;const existing=[...document.scripts].find(script=>script.src===resolved);if(existing?.dataset.cafarronLoaded==='true')return Promise.resolve();const promise=new Promise((resolve,reject)=>{const script=existing||document.createElement('script');let settled=false;const done=()=>{if(settled)return;settled=true;script.dataset.cafarronLoaded='true';resolve();};const fail=()=>{if(settled)return;settled=true;reject(new Error(`${src} could not be loaded.`));};script.addEventListener('load',done,{once:true});script.addEventListener('error',fail,{once:true});if(!existing){script.src=src;script.async=false;script.crossOrigin='anonymous';document.head.appendChild(script);}});scriptLoads.set(src,promise);promise.catch(()=>scriptLoads.delete(src));return promise;}async function loadThree(){if(window.THREE?.OrbitControls)return window.THREE;await loadScript(THREE_URL);await loadScript(ORBIT_URL);if(!window.THREE?.OrbitControls)throw new Error('Three-dimensional survey controls failed to initialize.');return window.THREE;}function clamp(value,min,max){return Math.max(min,Math.min(max,value));}function intersectArea(a,b){const width=Math.max(0,Math.min(a.right,b.right)-Math.max(a.left,b.left));const height=Math.max(0,Math.min(a.bottom,b.bottom)-Math.max(a.top,b.top));return width*height;}function expanded(rect,padding=5){return{left:rect.left-padding,right:rect.right+padding,top:rect.top-padding,bottom:rect.bottom+padding};}function rectFor(cx,cy,width,height){return{left:cx-width/2,right:cx+width/2,top:cy-height/2,bottom:cy+height/2};}function nearestPointOnRect(x,y,rect){return{x:clamp(x,rect.left,rect.right),y:clamp(y,rect.top,rect.bottom)};}function localCandidatePositions(x,y,width,height){const positions=[];for(const radius of[26,38,52,70,92,118]){for(const degrees of[-35,35,-145,145,-80,80,0,180]){const angle=degrees*Math.PI/180;positions.push({cx:x+Math.cos(angle)*(radius+width*.18),cy:y+Math.sin(angle)*(radius+height*.18)});}}return positions;}function routeStyle(layer){return{'major-warp':{color:0xd8b35e,opacity:.82,dashed:false,dashSize:0,gapSize:0},trade:{color:0x4fa3a5,opacity:.74,dashed:true,dashSize:2.8,gapSize:1.25},'local-navigation':{color:0x7390bd,opacity:.55,dashed:true,dashSize:1.4,gapSize:1.25},exploratory:{color:0xe2e5df,opacity:.22,dashed:true,dashSize:.75,gapSize:1.4}}[layer]||{color:0x817963,opacity:.42,dashed:true,dashSize:1.2,gapSize:1.2};}function routeMaterial(THREE,layer){const style=routeStyle(layer);if(!style.dashed){return new THREE.LineBasicMaterial({color:style.color,transparent:true,opacity:style.opacity});}return new THREE.LineDashedMaterial({color:style.color,transparent:true,opacity:style.opacity,dashSize:style.dashSize,gapSize:style.gapSize});}function buildRouteGroup(THREE,route,nodeById){const group=new THREE.Group();const nodes=route.nodeIds.map(id=>nodeById.get(id)).filter(Boolean);if(nodes.length<2)return group;for(let index=0;index<nodes.length-1;index+=1){const start=new THREE.Vector3(...nodes[index].position);const end=new THREE.Vector3(...nodes[index+1].position);const distance=start.distanceTo(end);const middle=start.clone().add(end).multiplyScalar(.5);const directionSign=index%2===0?1:-1;middle.y+=clamp(distance*.055,1.8,7.5);middle.z+=directionSign*clamp(distance*.028,.8,4.2);const curve=new THREE.QuadraticBezierCurve3(start,middle,end);const points=curve.getPoints(clamp(Math.round(distance*1.4),18,64));const geometry=new THREE.BufferGeometry().setFromPoints(points);const line=new THREE.Line(geometry,routeMaterial(THREE,route.layer));if(line.material.isLineDashedMaterial)line.computeLineDistances();line.userData.route=route;group.add(line);}return group;}async function mount(options){const{data,stage,labelLayer,leaderLayer,status,initialMode='orbit',onSelect}=options;const THREE=await loadThree();const nodeById=new Map(data.mapNodes.map(node=>[node.id,node]));const recordById=new Map(data.records.map(record=>[record.id,record]));const scene=new THREE.Scene();scene.background=new THREE.Color(0x030505);scene.fog=new THREE.FogExp2(0x030505,.0022);const camera=new THREE.PerspectiveCamera(48,1,.1,1100);const defaultTarget=new THREE.Vector3(18,0,0);const defaultPosition=new THREE.Vector3(38,72,170);camera.position.copy(defaultPosition);const renderer=new THREE.WebGLRenderer({antialias:true,powerPreference:'high-performance'});renderer.setPixelRatio(Math.min(window.devicePixelRatio||1,2));renderer.outputEncoding=THREE.sRGBEncoding;stage.insertBefore(renderer.domElement,leaderLayer);const controls=new THREE.OrbitControls(camera,renderer.domElement);controls.target.copy(defaultTarget);controls.enableDamping=true;controls.dampingFactor=.024;controls.rotateSpeed=.145;controls.panSpeed=.165;controls.zoomSpeed=.185;controls.keyPanSpeed=2.5;controls.enablePan=true;controls.screenSpacePanning=true;controls.minDistance=18;controls.maxDistance=340;controls.update();scene.add(new THREE.AmbientLight(0xb8aa85,.72));const key=new THREE.DirectionalLight(0xffdfa0,1.05);key.position.set(35,55,70);scene.add(key);const starGeometry=new THREE.BufferGeometry();const starPositions=[];for(let index=0;index<900;index+=1){starPositions.push((Math.random()-.5)*420,(Math.random()-.5)*220,(Math.random()-.5)*260);}starGeometry.setAttribute('position',new THREE.Float32BufferAttribute(starPositions,3));scene.add(new THREE.Points(starGeometry,new THREE.PointsMaterial({color:0xbcc5bf,size:.28,transparent:true,opacity:.48})));const grid=new THREE.GridHelper(260,26,0x5b5139,0x282923);grid.position.y=-34;grid.material.transparent=true;grid.material.opacity=.24;scene.add(grid);const groups={nodes:new THREE.Group(),regions:new THREE.Group(),hazards:new THREE.Group(),'route-major-warp':new THREE.Group(),'route-trade':new THREE.Group(),'route-local-navigation':new THREE.Group(),'route-exploratory':new THREE.Group()};Object.values(groups).forEach(group=>scene.add(group));const layerVisibility={primary:true,supporting:true,'guard-origin':true,provisional:false,unnamed:false,exploratory:true,regions:false,hazards:false,labels:true,'route-major-warp':true,'route-trade':true,'route-local-navigation':false,'route-exploratory':false};let interactionMode=VALID_MODES.has(initialMode)?initialMode:'orbit';let threatFilter='all';let selectedId='';let running=true;let frame=0;let labelDirty=true;let transition=null;let pointerStart=null;const nodeMeshes=new Map();const pickables=[];const labelRecords=new Map();const raycaster=new THREE.Raycaster();const pointer=new THREE.Vector2();function threatColor(node){return data.threatStates[node.threat]?.color||data.threatStates.unsurveyed.color;}function isNodeVisible(node){return layerVisibility[node.layer]!==false&&(threatFilter==='all'||node.threat===threatFilter);}function isCanonicalLabel(node){return node.layer==='primary'||node.layer==='supporting'||node.layer==='guard-origin';}function shouldShowLabel(node){if(!isNodeVisible(node)||!layerVisibility.labels)return false;if(node.id===selectedId)return true;return isCanonicalLabel(node);}for(const node of data.mapNodes){const scale=Math.max(.42,Number(node.scale||.8));const color=threatColor(node);const exploratory=node.layer==='exploratory';const guardOrigin=node.layer==='guard-origin';const geometry=new THREE.SphereGeometry(scale,18,14);const material=new THREE.MeshStandardMaterial({color,emissive:color,emissiveIntensity:exploratory?.11:guardOrigin?.24:.3,roughness:.5,metalness:.2,transparent:exploratory||guardOrigin,opacity:exploratory?.6:guardOrigin?.9:1});const mesh=new THREE.Mesh(geometry,material);mesh.position.set(...node.position);mesh.userData.nodeId=node.id;mesh.userData.baseScale=scale;groups.nodes.add(mesh);nodeMeshes.set(node.id,mesh);pickables.push(mesh);const halo=new THREE.Mesh(new THREE.SphereGeometry(scale*1.62,14,10),new THREE.MeshBasicMaterial({color,wireframe:true,transparent:true,opacity:exploratory?.07:guardOrigin?.13:.16}));halo.userData.nodeId=node.id;mesh.add(halo);const label=document.createElement('button');label.type='button';label.className='wh-map-label';label.dataset.nodeId=node.id;label.dataset.layer=node.layer;label.textContent=node.name;label.hidden=true;label.addEventListener('click',()=>selectNode(node.id,false));labelLayer.appendChild(label);const leader=document.createElementNS('http://www.w3.org/2000/svg','line');leader.setAttribute('stroke',data.threatStates[node.threat]?.css||'#f1f1ec');leader.setAttribute('stroke-width',guardOrigin?'.85':exploratory?'.7':'1');leader.setAttribute('stroke-opacity',guardOrigin?'.52':exploratory?'.36':'.66');leader.hidden=true;leaderLayer.appendChild(leader);labelRecords.set(node.id,{node,mesh,label,leader});}for(const route of data.routes){const key=`route-${route.layer}`;const targetGroup=groups[key];if(!targetGroup)continue;const routeGroup=buildRouteGroup(THREE,route,nodeById);routeGroup.userData.route=route;targetGroup.add(routeGroup);}function volume(record,group,opacity){const geometry=new THREE.SphereGeometry(1,22,14);const color=data.threatStates[record.threat]?.color||0xb49142;const material=new THREE.MeshBasicMaterial({color,wireframe:true,transparent:true,opacity});const mesh=new THREE.Mesh(geometry,material);mesh.position.set(...record.center);mesh.scale.set(...record.radii);mesh.userData.volume=record;group.add(mesh);}data.regions.forEach(region=>volume(region,groups.regions,.055));data.hazards.forEach(hazard=>volume(hazard,groups.hazards,.12));function modeDescription(){return{select:'Select mode: click contacts without moving the survey.',orbit:'Grab / orbit mode: drag slowly around the sector.',pan:'Pan mode: drag the survey plane with graduated resistance.',zoom:'Zoom mode: drag vertically or use the wheel / pinch lens.'}[interactionMode];}function updateStatus(){status.textContent=`${data.mapNodes.length} plotted contacts · ${modeDescription()} `+`Guard-origin worlds remain listed; exploratory names remain sealed until selected.`;}function applyInteractionMode(){stage.dataset.mapMode=interactionMode;controls.enabled=interactionMode!=='select'&&!transition;if(interactionMode==='orbit'){controls.mouseButtons.LEFT=THREE.MOUSE.ROTATE;controls.mouseButtons.MIDDLE=THREE.MOUSE.DOLLY;controls.mouseButtons.RIGHT=THREE.MOUSE.PAN;controls.touches.ONE=THREE.TOUCH.ROTATE;controls.touches.TWO=THREE.TOUCH.DOLLY_PAN;}else if(interactionMode==='pan'){controls.mouseButtons.LEFT=THREE.MOUSE.PAN;controls.mouseButtons.MIDDLE=THREE.MOUSE.DOLLY;controls.mouseButtons.RIGHT=THREE.MOUSE.ROTATE;controls.touches.ONE=THREE.TOUCH.PAN;controls.touches.TWO=THREE.TOUCH.DOLLY_PAN;}else if(interactionMode==='zoom'){controls.mouseButtons.LEFT=THREE.MOUSE.DOLLY;controls.mouseButtons.MIDDLE=THREE.MOUSE.DOLLY;controls.mouseButtons.RIGHT=THREE.MOUSE.PAN;controls.touches.ONE=THREE.TOUCH.PAN;controls.touches.TWO=THREE.TOUCH.DOLLY_PAN;}updateStatus();}function updateVisibility(){for(const[id,mesh]of nodeMeshes){mesh.visible=isNodeVisible(nodeById.get(id));}groups.regions.visible=layerVisibility.regions;groups.hazards.visible=layerVisibility.hazards;for(const key of['route-major-warp','route-trade','route-local-navigation','route-exploratory']){groups[key].visible=layerVisibility[key];}labelLayer.hidden=!layerVisibility.labels;leaderLayer.hidden=!layerVisibility.labels;labelDirty=true;}function projectNode(node){const vector=new THREE.Vector3(...node.position).project(camera);const rect=stage.getBoundingClientRect();return{x:(vector.x*.5+.5)*rect.width,y:(-vector.y*.5+.5)*rect.height,z:vector.z,width:rect.width,height:rect.height};}function hideLabel(record){record.label.hidden=true;record.leader.hidden=true;}function measureCandidate(record,projected){const{node,label}=record;label.hidden=false;label.style.visibility='hidden';label.style.left='0px';label.style.top='0px';const width=Math.min(Math.max(label.offsetWidth||node.name.length*7+18,62),218);const height=Math.max(label.offsetHeight||25,24);return{...record,...projected,labelWidth:width,labelHeight:height};}function viewportTopInset(){const consoleElement=stage.querySelector('.wh-viewport-console');if(!consoleElement)return 10;return Math.max(10,consoleElement.offsetTop+consoleElement.offsetHeight+10);}function laneX(side,lane,stageWidth,labelWidth){const margin=10;const inset=lane===0?0:226;return side==='left'?margin+inset+labelWidth/2:stageWidth-margin-inset-labelWidth/2;}function placeEdgeGroup(items,side,lane,stageWidth,stageHeight,topInset){if(!items.length)return[];const bottomMargin=10;const available=Math.max(1,stageHeight-topInset-bottomMargin);const totalLabelHeight=items.reduce((sum,item)=>sum+item.labelHeight,0);const gap=items.length>1?Math.max(1,Math.min(7,(available-totalLabelHeight)/(items.length-1))):0;const sorted=[...items].sort((a,b)=>a.y-b.y);const placed=[];let cursor=topInset;for(const item of sorted){const ideal=clamp(item.y,topInset+item.labelHeight/2,stageHeight-bottomMargin-item.labelHeight/2);const cy=Math.max(ideal,cursor+item.labelHeight/2);const cx=laneX(side,lane,stageWidth,item.labelWidth);const rect=rectFor(cx,cy,item.labelWidth,item.labelHeight);placed.push({...item,cx,cy,rect,placement:`edge-${side}-${lane}`});cursor=rect.bottom+gap;}const overflow=placed.length?placed[placed.length-1].rect.bottom-(stageHeight-bottomMargin):0;if(overflow>0){for(const item of placed){item.cy-=overflow;item.rect=rectFor(item.cx,item.cy,item.labelWidth,item.labelHeight);}}for(let index=placed.length-2;index>=0;index-=1){const current=placed[index];const next=placed[index+1];const maximumBottom=next.rect.top-gap;if(current.rect.bottom>maximumBottom){current.cy-=current.rect.bottom-maximumBottom;current.rect=rectFor(current.cx,current.cy,current.labelWidth,current.labelHeight);}}return placed;}function placeSelectedLocal(item,occupied,stageWidth,stageHeight,topInset){const margin=7;for(const position of localCandidatePositions(item.x,item.y,item.labelWidth,item.labelHeight)){const rect=rectFor(position.cx,position.cy,item.labelWidth,item.labelHeight);const inside=rect.left>=margin&&rect.top>=topInset&&rect.right<=stageWidth-margin&&rect.bottom<=stageHeight-margin;if(!inside)continue;if(occupied.some(entry=>intersectArea(expanded(rect),entry)>0))continue;return{...item,...position,rect,placement:'local'};}const side=item.x<=stageWidth/2?'left':'right';return placeEdgeGroup([item],side,1,stageWidth,stageHeight,topInset)[0]||null;}function applyLabelPlacement(item){const{node,label,leader,x,y,cx,cy,rect,placement}=item;label.hidden=false;label.style.visibility='visible';label.style.left=`${cx}px`;label.style.top=`${cy}px`;label.dataset.placement=placement;label.setAttribute('aria-current',node.id===selectedId?'true':'false');const end=nearestPointOnRect(x,y,rect);leader.hidden=false;leader.setAttribute('x1',String(x));leader.setAttribute('y1',String(y));leader.setAttribute('x2',String(end.x));leader.setAttribute('y2',String(end.y));}function layoutLabels(){if(!layerVisibility.labels)return;const stageRect=stage.getBoundingClientRect();if(!stageRect.width||!stageRect.height)return;const topInset=viewportTopInset();const primary=[];const guardOrigins=[];let selectedOther=null;for(const record of labelRecords.values()){const{node}=record;if(!shouldShowLabel(node)){hideLabel(record);continue;}const projected=projectNode(node);if(projected.z<-1||projected.z>1||projected.x<-60||projected.x>projected.width+60||projected.y<-60||projected.y>projected.height+60){hideLabel(record);continue;}const candidate=measureCandidate(record,projected);if(node.layer==='guard-origin')guardOrigins.push(candidate);else if(node.layer==='primary'||node.layer==='supporting')primary.push(candidate);else if(node.id===selectedId)selectedOther=candidate;}const placements=[];if(stageRect.width<760){const combined=[...primary,...guardOrigins].sort((a,b)=>{const aPriority=a.node.layer==='guard-origin'?1:0;const bPriority=b.node.layer==='guard-origin'?1:0;return aPriority-bPriority||a.y-b.y;});const left=combined.filter(item=>item.x<=stageRect.width/2);const right=combined.filter(item=>item.x>stageRect.width/2);placements.push(...placeEdgeGroup(left,'left',0,stageRect.width,stageRect.height,topInset),...placeEdgeGroup(right,'right',0,stageRect.width,stageRect.height,topInset));}else{for(const[items,lane]of[[primary,0],[guardOrigins,1]]){const left=items.filter(item=>item.x<=stageRect.width/2);const right=items.filter(item=>item.x>stageRect.width/2);placements.push(...placeEdgeGroup(left,'left',lane,stageRect.width,stageRect.height,topInset),...placeEdgeGroup(right,'right',lane,stageRect.width,stageRect.height,topInset));}}const occupied=placements.map(item=>expanded(item.rect));placements.forEach(applyLabelPlacement);if(selectedOther){const placement=placeSelectedLocal(selectedOther,occupied,stageRect.width,stageRect.height,topInset);if(placement)applyLabelPlacement(placement);else hideLabel(selectedOther);}labelDirty=false;}function renderSelection(node){selectedId=node.id;for(const[id,mesh]of nodeMeshes){const selected=id===selectedId;mesh.scale.setScalar(selected?1.55:1);const record=nodeById.get(id);mesh.material.emissiveIntensity=selected?.85:record.layer==='exploratory'?.11:record.layer==='guard-origin'?.24:.3;}labelRecords.forEach((record,id)=>{record.label.setAttribute('aria-current',id===selectedId?'true':'false');});const connectedRoutes=data.routes.filter(route=>route.nodeIds.includes(node.id));onSelect?.(node,node.recordIds.map(id=>recordById.get(id)).filter(Boolean),connectedRoutes);labelDirty=true;}function easeGelatin(value){return value<.5?4*value*value*value:1-Math.pow(-2*value+2,3)/2;}function beginTransition(position,target,duration=1750){transition={startTime:performance.now(),duration,fromPosition:camera.position.clone(),toPosition:position.clone(),fromTarget:controls.target.clone(),toTarget:target.clone()};controls.enabled=false;labelDirty=true;}function updateTransition(now){if(!transition)return;const progress=clamp((now-transition.startTime)/transition.duration,0,1);const eased=easeGelatin(progress);camera.position.lerpVectors(transition.fromPosition,transition.toPosition,eased);controls.target.lerpVectors(transition.fromTarget,transition.toTarget,eased);labelDirty=true;if(progress>=1){transition=null;applyInteractionMode();}}function focusNode(node){const target=new THREE.Vector3(...node.position);const direction=camera.position.clone().sub(controls.target);if(direction.lengthSq()<.001)direction.set(0,.35,1);direction.normalize();const currentDistance=camera.position.distanceTo(controls.target);const distance=Math.max(30,currentDistance*.48);camera.up.set(0,1,0);beginTransition(target.clone().add(direction.multiplyScalar(distance)),target,1650);}function selectNode(nodeId,focus=false){const node=nodeById.get(nodeId);if(!node)return;renderSelection(node);if(focus)focusNode(node);}function pickAt(event){const rect=renderer.domElement.getBoundingClientRect();pointer.x=((event.clientX-rect.left)/rect.width)*2-1;pointer.y=-((event.clientY-rect.top)/rect.height)*2+1;raycaster.setFromCamera(pointer,camera);const hit=raycaster.intersectObjects(pickables.filter(mesh=>mesh.visible),false)[0];if(hit?.object?.userData?.nodeId)selectNode(hit.object.userData.nodeId,false);}renderer.domElement.addEventListener('pointerdown',event=>{pointerStart={x:event.clientX,y:event.clientY,time:performance.now()};stage.dataset.dragging='true';});renderer.domElement.addEventListener('pointerup',event=>{stage.dataset.dragging='false';if(event.button!==0||!pointerStart){pointerStart=null;return;}const distance=Math.hypot(event.clientX-pointerStart.x,event.clientY-pointerStart.y);const duration=performance.now()-pointerStart.time;pointerStart=null;if(interactionMode==='select'||(distance<=5&&duration<500))pickAt(event);});renderer.domElement.addEventListener('pointercancel',()=>{stage.dataset.dragging='false';pointerStart=null;});controls.addEventListener('change',()=>{labelDirty=true;});function resize(){const width=Math.max(320,stage.clientWidth);const height=Math.max(420,stage.clientHeight);renderer.setSize(width,height,false);camera.aspect=width/height;camera.updateProjectionMatrix();leaderLayer.setAttribute('viewBox',`0 0 ${width} ${height}`);labelDirty=true;}const resizeObserver=new ResizeObserver(resize);resizeObserver.observe(stage);resize();function animate(now=performance.now()){if(!running)return;requestAnimationFrame(animate);updateTransition(now);controls.update();renderer.render(scene,camera);frame+=1;if(labelDirty||frame%12===0)layoutLabels();}applyInteractionMode();updateVisibility();animate();return{selectNode,setMode(mode){if(!VALID_MODES.has(mode))return;interactionMode=mode;applyInteractionMode();},setLayer(layer,visible){if(!(layer in layerVisibility))return;layerVisibility[layer]=Boolean(visible);updateVisibility();},setThreat(value){threatFilter=value||'all';updateVisibility();},reset(){camera.up.set(0,1,0);beginTransition(defaultPosition.clone(),defaultTarget.clone(),1900);},top(){camera.up.set(0,0,-1);const target=defaultTarget.clone();const position=new THREE.Vector3(target.x,230,target.z+.01);beginTransition(position,target,1900);setTimeout(()=>{if(!transition)camera.up.set(0,1,0);},2000);},pause(){running=false;},resume(){if(running)return;running=true;labelDirty=true;animate();},destroy(){running=false;resizeObserver.disconnect();renderer.dispose();renderer.domElement.remove();labelLayer.replaceChildren();leaderLayer.replaceChildren();}};}window.CafarronSectorMap=Object.freeze({mount});})();